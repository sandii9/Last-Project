# Dicoding Web Intermediate — Proyek Awal (SPA + Map + Camera) — **Vite Bundler**
Lihat **README** di root untuk cara menjalankan.

## Jalankan
```bash
npm install
npm run dev
# build produksi
npm run build
npm run preview
```
